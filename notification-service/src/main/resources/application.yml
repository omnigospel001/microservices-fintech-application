spring:
  config:
    import: ${CONFIG_SERVER_URI:optional:configserver:http://localhost:8888}
  application:
    name: notification-service
  mail:
    company:
      name: Gospel's Fintech App

Debug: true